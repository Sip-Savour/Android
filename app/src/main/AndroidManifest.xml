<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SipNSavour"
        tools:targetApi="31">

        <!-- Splash Activity (FIRST) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Weekly Choice Activity -->
        <activity
            android:name=".WeeklyChoiceActivity"
            android:exported="false" />

        <!-- Choose Flavors Activity -->
        <activity
            android:name=".ChooseFlavorsActivity"
            android:exported="false" />

        <!-- Search Activity -->
        <activity
            android:name=".SearchActivity"
            android:exported="false" />

        <!-- Wine List Activity -->
        <activity
            android:name=".WineListActivity"
            android:exported="false" />

        <!-- Wine Info Activity -->
        <activity
            android:name=".WineInfoActivity"
            android:exported="false" />

        <!-- Suggestion Activity -->
        <activity
            android:name=".SuggestionActivity"
            android:exported="false" />

        <!-- Favorites Activity -->
        <activity
            android:name=".FavoritesActivity"
            android:exported="false" />

        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".EditProfileActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".PreferencesActivity"
            android:exported="false" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false" />
    </application>

</manifest>